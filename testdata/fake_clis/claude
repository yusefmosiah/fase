#!/bin/sh
set -eu

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
if [ "${1:-}" = "--version" ] || [ "${1:-}" = "-v" ]; then
  printf '%s\n' 'fake-claude 0.0.1'
  exit 0
fi
cat "$script_dir/../fixtures/claude/success.jsonl"
